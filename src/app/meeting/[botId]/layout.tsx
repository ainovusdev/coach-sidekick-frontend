/**
 * Coach Meeting Layout
 * WebSocketProvider is now in root layout for global processing notifications
 */

export default function MeetingLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
